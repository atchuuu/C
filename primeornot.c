#include<stdio.h>
int main()
{
int a,b,i;
printf("enter a number =");
scanf("%d",&a);
for (i=1;i<=a;i++)
{
    if (a%i==0)
    {
        b==1;
    }

}
if (b==1)
    printf("It is a composite number");
else
    printf("it is a prime number");
}
